interface:
  display_name: "Sip"
  short_description: "Publish cups and manage brew setups"
  default_prompt: "Use $sip to publish this cup or brew setup, update its rotation status, and keep a private receipt."
